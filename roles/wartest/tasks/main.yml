
-name: run shell command for Gitlab athentication
  ansible.builtin.shell: "docker login registry.example.com -u 'WhiteWolf' -p 'MRgithub6726!!'"
  register: login_result
  ignore_errors: yes

- name: run container
  docker_container:
    name: wartest
    image: "{{ image }}"
    ports: 8081:8080
    state: started
 
