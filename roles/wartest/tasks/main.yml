
- name: run shell command for Gitlab athentication
  shell: "docker login registry.gitlab.com"
  register: login_result
  ignore_errors: yes

- name: run container
  docker_container:
    name: wartest
    image: "{{ image }}"
    ports: 8081:8080
    state: started
 
