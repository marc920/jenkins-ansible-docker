
- name: run shell command for Gitlab authentication
  shell: "docker login registry.gitlab.com -u 'marc@rimbaud.net' -p 'MRgithub6726!!'"
  register: login_result
  ignore_errors: yes

- name: run container
  docker_container:
    name: wartest
    image: "{{ image }}"
    ports: 8081:8080
    state: started
 
